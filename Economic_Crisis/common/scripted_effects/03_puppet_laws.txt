clear_switcher = {
	# if = {
	# 	limit = { check_variable = { puppet_laws_status_state = 1 } }
	# 	clear_variable = puppet_laws_status_state
	# }
	if = {
		limit = { check_variable = { puppet_laws_corruption = 1 } }
		clear_variable = puppet_laws_corruption
	}
	if = {
		limit = { check_variable = { puppet_laws_terrorism = 1 } }
		clear_variable = puppet_laws_terrorism
	}
	if = {
		limit = { check_variable = { puppet_laws_ecology = 1 } }
		clear_variable = puppet_laws_ecology
	}
	if = {
		limit = { check_variable = { puppet_laws_integration = 1 } }
		clear_variable = puppet_laws_integration
	}
	if = {
		limit = { check_variable = { puppet_laws_status_eu = 1 } }
		clear_variable = puppet_laws_status_eu
	}
	# if = {
	# 	limit = { check_variable = { puppet_laws_state = 1 } }
	# 	clear_variable = puppet_laws_state
	# }
	if = {
		limit = { check_variable = { puppet_laws_admin = 1 } }
		clear_variable = puppet_laws_admin
	}
	if = {
		limit = { check_variable = { puppet_laws_religion = 1 } }
		clear_variable = puppet_laws_religion
	}
	if = {
		limit = { check_variable = { puppet_laws_media = 1 } }
		clear_variable = puppet_laws_media
	}
	if = {
		limit = { check_variable = { puppet_laws_civil_liberties = 1 } }
		clear_variable = puppet_laws_civil_liberties
	}
	if = {
		limit = { check_variable = { puppet_laws_army = 1 } }
		clear_variable = puppet_laws_army
	}
	if = {
		limit = { check_variable = { puppet_laws_schools = 1 } }
		clear_variable = puppet_laws_schools
	}
	if = {
		limit = { check_variable = { puppet_laws_migration = 1 } }
		clear_variable = puppet_laws_migration
	}
	if = {
		limit = { check_variable = { puppet_laws_economy = 1 } }
		clear_variable = puppet_laws_economy
	}
	if = {
		limit = { check_variable = { puppet_laws_market = 1 } }
		clear_variable = puppet_laws_market
	}
	if = {
		limit = { check_variable = { puppet_laws_focus = 1 } }
		clear_variable = puppet_laws_focus
	}
	if = {
		limit = { check_variable = { puppet_laws_mobilisation = 1 } }
		clear_variable = puppet_laws_mobilisation
	}
	if = {
		limit = { check_variable = { puppet_laws_taxes = 1 } }
		clear_variable = puppet_laws_taxes
	}
	if = {
		limit = { check_variable = { puppet_laws_security = 1 } }
		clear_variable = puppet_laws_security
	}
	if = {
		limit = { check_variable = { puppet_laws_pensions = 1 } }
		clear_variable = puppet_laws_pensions
	}
	if = {
		limit = { check_variable = { puppet_laws_trade = 1 } }
		clear_variable = puppet_laws_trade
	}
	
}

initialization_of_start_variables = {
	#add_to_variable = { puppet_laws_status_state = 0 }
	add_to_variable = { puppet_laws_corruption = 0 }
	add_to_variable = { puppet_laws_terrorism = 0 }
	add_to_variable = { puppet_laws_ecology = 0 }
	add_to_variable = { puppet_laws_integration = 0 }
	add_to_variable = { puppet_laws_status_eu = 0 }
	#add_to_variable = { puppet_laws_state = 0 }
	add_to_variable = { puppet_laws_admin = 0 }
	add_to_variable = { puppet_laws_religion = 0 }
	add_to_variable = { puppet_laws_media = 0 }
	add_to_variable = { puppet_laws_civil_liberties = 0 }
	add_to_variable = { puppet_laws_army = 0 }
	add_to_variable = { puppet_laws_schools = 0 }
	add_to_variable = { puppet_laws_migration = 0 }
	add_to_variable = { puppet_laws_economy = 0 }
	add_to_variable = { puppet_laws_market = 0 }
	add_to_variable = { puppet_laws_focus = 0 }
	add_to_variable = { puppet_laws_mobilisation = 0 }
	add_to_variable = { puppet_laws_taxes = 0 }
	add_to_variable = { puppet_laws_security = 0 }
	add_to_variable = { puppet_laws_pensions = 0 }
	add_to_variable = { puppet_laws_trade = 0 }
}