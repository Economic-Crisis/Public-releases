# autonomy_state = {
	# id = autonomous_area	# id =  системное название уровня автономности
	
	# default = yes # входит ли в дефолтную цепочку освобождения / поглощения (НУЖНО ПОИГРАТЬ И ПОСОМТРЕТЬ!)
	# is_puppet = yes # является марионеткой
	
	# min_freedom_level = 0.0 # уровень свободы на котором происходит переход к большей свободе
	
	# manpower_influence = 0.9 # коэффициент (влияние?) на численность (прирост?) насиления марионетки (НУЖНО ПОИГРАТЬ И ПОСОМТРЕТЬ!)
	
	# rule = {
		# can_not_declare_war = yes # не может объявлять войну сам
		# can_decline_call_to_war = no # может отклонять требование мажора вступить в войну
		# units_deployed_to_overlord = yes # управляет ли мажор развернутыми войсками
		# can_send_volunteers = no # может ли отправлять волонетров
		# can_guarantee_other_ideologies = no # может гарантировать независимость другой стране
	# }
	
	# modifier = {
		# autonomy_manpower_share = 0.85 # доля населения, отдаваемая (?) мажору
		# can_master_build_for_us = 1 # может ли мажор строить на нашей территории
		# conscription_factor = -0.5 # коэффициент призывников - увеличение или уменьшение числа призывников у марионетки
		# extra_trade_to_overlord_factor = 1.0 # % излишков сырья отдаваемые мажору
		# overlord_trade_cost_factor = -0.9 # фaктор стоимости нашей продукции для мажора
		# cic_to_overlord_factor = 0.50 # % фабрик, работающих на мажора
		# mic_to_overlord_factor = 0.75 # % военных заводов, работающих на мажора
		# license_subject_master_purchase_cost = -1 # стоимость получение лицензии от мажора
		# research_sharing_per_country_bonus_factor = -0.5 # бонус от времени на совместное с мажором исслеование технологий
		# master_ideology_drift = 0.3 # рост у марионетки влияния идеологии государтсва - мажора
		# drift_defence_factor = 0.1 # защита от смены идеологии у марионетки
		# autonomy_gain = 2 # увелчение уровня автономности марионетки (?)
		# autonomy_gain_global_factor = -0.3 # влияние на (НУЖНО ПОИГРАТЬ И ПОСОМТРЕТЬ!)
	# }
	
	# # дрфит для АИ на полную независимость
	# ai_subject_wants_higher = {
		# factor = 1.0
	# }
	
	# # дрифт для АИ на сближение с мажором
	# ai_overlord_wants_lower = {
		# factor = 0.0
	# }

	# # запрашивает ли АИ у марионетки войска
	# ai_overlord_wants_garrison = {
		# always = yes
	# }

	# allowed = {
		# has_dlc = "Together for Victory"
		# if = {
			# OR = {
				# is_subject = no # марионетка вообще не марионетка? (НУЖНО ПОИГРАТЬ И ПОСОМТРЕТЬ!)
				# has_autonomy_state = autonomy_state # марионетка УЖЕ имеет статус? (НУЖНО ПОИГРАТЬ И ПОСОМТРЕТЬ!)
			# }
			# limit = { has_dlc = "Waking the Tiger" } # только с ДЛЦ
			# AND = {
				# NOT = { has_government = rad_islamists } # этот тип марионетки будет использоваться только НЕ для идеологии
				# NOT = { OVERLORD = { original_tag = IRN } }  # этот тип марионетки будет использоваться только НЕ для страны
			# }
		# }
	# }
	
	# # перечень связанных уровней марионетки?
	# allowed_levels_filter = {
		# autonomy_supervised_state
	# }	
	
	# #ROOT is puppet - FROM is potential OVERLORD
	# use_for_peace_conference_weight = {
		# base = 0
		# modifier = {
			# add = 5
			# has_dlc = "Man the Guns"
			# FROM = {
				# has_government = democratic 
			# }
		# }
	# }
	
	# can_take_level = {
		# #trigger here может быть always = yes
		# if = {
			# limit = { original_tag = IRN }
			# OR = {
				# has_completed_focus = IRN_goal_islamisation # лучше не ссылаться на фокусы
				# has_country_flag = IRN_flag_islamisation # а вот на флаги ссылаться хорошо
			# }
		# }
	# }

	# can_lose_level = {
		# #trigger here
		# always = no
	# }
# }